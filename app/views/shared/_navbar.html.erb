<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Task Assignation</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
      <% if user_signed_in? && current_user.role == 'admin'%> 
        <a class="nav-link" href="show_admin_tasks">Tasks</a>
        <%end%>
      </li>
      </ul>
      
      <ul class="navbar-nav ml-auto"> 
        <li class="nav-item">
          <% if user_signed_in? %>  
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: :"btn btn-primary" %>
          <% else %>
            <a class="nav-link" href="/users/sign_in">Sign in</a>
            <li><a class="nav-link" href="/users/sign_up">Sign up</a></li>
          <% end %>
        </li>
        
      </ul>
      
    
  </div>
</nav>